format_version: 5

envs:
  - TEST_WORKFLOWS: $TEST_WORKFLOWS
  - ACCESS_TOKEN: $ACCESS_TOKEN

workflows:
  test:
    steps:
    - path::./:
        inputs:
        - workflows: $TEST_WORKFLOWS
        - access_token: $ACCESS_TOKEN
        - wait_for_builds: "true"